Echo ON
REM parameters to be used by the Installer
--serviceType "{Worker or Controller}" --clusterName "{Server Farm or Domain name}" --controllerMachineName "{Name of Machine that has the SharedDirectory}" --destinationDirectory "{Where the folders will be deployed to}" --sourceDirectory "{Where the files of the program are coming from}" --serviceAccountName "{Domain or Local account}" --serviceAccountPassword "{Password}"