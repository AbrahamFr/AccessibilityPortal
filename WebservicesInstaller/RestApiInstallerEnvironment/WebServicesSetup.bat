"<path to \RestApiInstallerEnvironment.exe>" --targetAppName <target web service name> --clusterName <cluster name> --serviceAccountName <application pool username> --serviceAccountPassword <application pool password> --sourceDirectory <path to source files> --destinationDirectory <directory location of files to be deployed> --controllerMachineName <server name of shared folder> --queueServiceSourceDirectory <directory of source files for Queue Server> --databaseAdminName <(optional - pass only if you don't pass useWindowsAuthentication) admin db name> --databaseAdminPassword <(optional - pass only if you don't pass useWindowsAuthentication) admin db password> --useWindowsAuthentication <optional parameter, if you pass then don't pass databaseAdminName and pass databaseAdminPassword>
